import { h } from "@stencil/core";
export const Lights = ({ ambientLightColor, ambientLightIntensity }, _children) => (h("a-entity", { id: "ambient-light", light: `type: ambient; color: ${ambientLightColor}; intensity: ${ambientLightIntensity}` }));
