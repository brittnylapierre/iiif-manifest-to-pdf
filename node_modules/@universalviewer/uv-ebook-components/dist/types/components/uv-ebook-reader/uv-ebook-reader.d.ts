import { EventEmitter } from "../../stencil.core";
export declare class UvEbookReader {
    private _book;
    private _rendition;
    private _viewer;
    private _href;
    private _bookPath;
    private _bookReady;
    private _prevEnabled;
    private _mobile;
    private _nextEnabled;
    private _showDivider;
    width: string;
    height: string;
    mobileWidth: number;
    minSpreadWidth: number;
    el: HTMLElement;
    load(url: string): Promise<void>;
    resize(): Promise<void>;
    display(href: string): Promise<void>;
    getBook(): Promise<any>;
    bookReady: EventEmitter;
    loadedBookMetadata: EventEmitter;
    loadedNavigation: EventEmitter;
    relocated: EventEmitter;
    renditionAttached: EventEmitter;
    handleKeyDown(ev: KeyboardEvent): void;
    private _prev;
    private _next;
    private _prevButtonClickedHandler;
    private _nextButtonClickedHandler;
    render(): void;
    componentDidUpdate(): void;
}
